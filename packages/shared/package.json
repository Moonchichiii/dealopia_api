{
  "name": "@dealopia/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.js",
    "./types": "./types/index.ts",
    "./constants": "./constants/index.ts"
  },
  "files": [
    "src",
    "types",
    "constants"
  ],
  "scripts": {
    "build": "echo 'shared package build pending integration'",
    "test": "echo 'shared package tests pending integration'",
    "lint": "echo 'shared package lint pending integration'",
    "typecheck": "echo 'shared package typecheck pending integration'",
    "format": "echo 'shared package format pending integration'"
  }
}
